# AGENTS.md

## プロジェクト概要

このプロジェクトは、macOS / Windows向けのFlutterデスクトップ音楽管理アプリである。

主な目的は、ローカルに保存された音楽ファイルを管理し、ユーザーが楽曲を検索・整理・再生できるようにすることである。

## 要件定義

プロダクトの要件は [`docs/requirements.md`](docs/requirements.md) に定義する。

実装や設計を行う前に、関連する要件を確認すること。

要件と実装方針に矛盾がある場合は、独断で要件を変更せず、以下を確認する。

- 対象となる要件
- 矛盾している内容
- 実装への影響
- 必要な要件変更案

要件を変更する場合は、`docs/requirements.md`を更新し、変更内容を作業報告に記載すること。

MVPの範囲外にある機能は、明示的な指示がない限り実装しない。

## 機能別仕様書

機能を追加・変更する場合は、`docs/features/`配下に機能別のディレクトリを作成する。

ディレクトリ名は、作成時点のローカル時刻を使い、次の形式とする。

```text
YYMMDDHHMM-<spec_name>
```

`<spec_name>`は英小文字とハイフン区切りで記載する。例:

```text
docs/features/2607311830-folder-registration/
```

各機能別ディレクトリには、次のファイルを配置する。

- `spec.md`: 機能の目的、対象範囲、要件、完了条件、必要に応じた設計メモ
- `tasks.md`: 実装、テスト、動作確認などのタスク

実装前に`spec.md`と`tasks.md`を作成し、実装内容や完了条件に変更があった場合は文書も更新する。

プロダクト全体の要件は`docs/requirements.md`に記載し、機能固有の詳細は各機能の`spec.md`に記載する。

Generator-Evaluator運用の評価基準は[`docs/harness/evaluator-rubric.md`](docs/harness/evaluator-rubric.md)、スプリント契約のテンプレートは[`docs/harness/sprint-contract-template.md`](docs/harness/sprint-contract-template.md)を参照する。`spec.md`は要件、`sprint-contract.md`は観測する振る舞い、`tasks.md`は実装・テスト作業を定義する。

## 対象プラットフォーム

- macOS
- Windows

Web、iOS、Androidは、明示的な指示がない限り対象外とする。

## 基本方針

- 実装後は必ず動作確認を行う
- 動作確認できていない変更を完了扱いにしない
- 変更範囲を必要最小限にする
- 既存の設計・命名・実装パターンを尊重する
- 不明点や未実装部分を、実装済みであるかのように報告しない
- ユーザーの音楽ファイルを意図せず破壊・移動・削除しない

## 技術スタック

- UI: Flutter
- 言語: Dart
- 状態管理: `<未決定>`
- 永続化: `<未決定>`
- 音声再生: `<未決定>`
- メタデータ解析: `<未決定>`

新しいパッケージを追加する場合は、必要性、対象プラットフォーム対応状況、保守性を確認する。

## アーキテクチャ

詳細なアーキテクチャと依存関係のルールは [`docs/architecture.md`](docs/architecture.md) を参照する。

- UI、アプリケーションロジック、データアクセスを分離する
- Widgetから直接ファイルシステムやデータベースへアクセスしない
- macOS / Windows固有の処理は、適切なサービスまたは抽象化層に隔離する
- 外部依存を差し替えられる構造にし、ロジックをテストしやすくする
- 既存のアーキテクチャが定まっている場合は、それを優先する

## 音楽ファイルの扱い

- ユーザーのファイルを勝手に削除、移動、上書きしない
- ファイル操作の前に対象パスと操作内容を検証する
- 大量の音楽ファイルを扱ってもUIスレッドをブロックしない
- ファイル読み込みやメタデータ解析は非同期で行う
- macOS / Windowsのパス、権限、ファイル名の差異を考慮する
- テストでは実ユーザーのライブラリを使用せず、テスト用の一時データを使用する

## UI / UX

- macOS / Windowsの両方で自然に操作できるUIを目指す
- ウィンドウサイズ変更に対応する
- キーボード操作を考慮する
- ローディング、空状態、エラー状態を明示する
- 大量の楽曲一覧でも操作性を維持する
- 破壊的な操作には確認を表示する

## テスト方針

### 単体テスト

検索、ソート、フィルタリング、メタデータ変換、再生キューなどのロジックには単体テストを追加する。

### Widgetテスト

個別のWidgetや画面状態、ユーザー操作に対するUIの変化をWidgetテストで検証する。

### Integration Test

画面遷移や複数の機能をまたぐ主要フローには、Flutter公式の `integration_test` を使用する。

最低限、次のフローを対象とする。

- アプリが起動する
- 音楽フォルダを登録できる
- 音楽ファイルがライブラリに表示される
- 楽曲を検索できる
- 楽曲を選択して再生できる
- アプリ再起動後も必要なデータが保持される

## 実装後の動作確認

実装または修正後、変更内容に応じて以下を実行する。

```bash
flutter analyze
flutter test
```

UI、画面遷移、ファイル読み込み、再生などに変更がある場合は、Integration Testも実行する。

macOSでは次を実行する。

```bash
flutter test integration_test -d macos
```

Windowsでは次を実行する。

```bash
flutter test integration_test -d windows
```

実行できない環境では、実行できなかった理由を報告する。
テスト未実行の場合、「動作確認済み」と報告してはならない。

## コマンド

プロジェクトの標準コマンドは、プロジェクト構成が確定した時点で追記する。

```bash
# 依存関係の取得
flutter pub get

# 静的解析
flutter analyze

# 単体テスト・Widgetテスト
flutter test

# macOSで起動
flutter run -d macos

# Windowsで起動
flutter run -d windows
```

## コーディングルール

- Dartのnull safetyを使用する
- 静的解析の警告を残さない
- 既存の命名規則とフォーマットに従う
- 1つの変更で無関係なリファクタリングを行わない
- TODOを追加する場合は、理由や前提条件をコメントに残す
- エラーを握りつぶさず、ユーザーまたは呼び出し元が扱える形で返す

## 変更報告

変更完了時には、以下を簡潔に報告する。

- 実施した変更
- 実行したテスト・動作確認
- 実行できなかった確認とその理由
- 残っている課題や注意点
