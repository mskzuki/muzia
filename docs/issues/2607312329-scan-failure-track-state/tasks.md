# スキャン失敗後に楽曲一覧が中途半端な状態で残る 対応タスク

## 実装

- [x] スキャン失敗時に楽曲一覧を永続層の内容へ戻す
- [x] 解析全件失敗の分岐でも同様に戻す

## テスト

- [x] スキャン失敗後の `tracks` が永続層と一致することをテストする
- [x] `flutter analyze` と `flutter test` を実行する

## 完了

- [x] 完了条件と未実行確認を記録する

### 実行した確認

- `flutter analyze`: 警告なし
- `flutter test`: 全件成功

### 補足

- 解析全件失敗の分岐は `TrackFound` を受け取らないため、実際には `_tracks` が
  書き換わっていなかった。不変条件を明示するため、同じ復元処理を通す。
