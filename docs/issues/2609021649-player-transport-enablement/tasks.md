# タスク

前提: [spec.md](spec.md) の確認事項1〜2の方針についてユーザーの合意を得る。
→ 2026-09-18 に以下の方針で着手（作業報告に記載。異なる判断があれば見直す）。

- 確認事項1: デザインハンドオフ適合として実装し、FR-009 に拡張操作（シーク・前後スキップ・音量）を追記した。
- 確認事項2: 「再生開始時点の表示中一覧（検索・ソート適用後）の順序」を暗黙のキューとする。
  アーティスト/アルバムブラウザではヒーローの再生・行の再生とも、表示中の楽曲リストがキューになる。
  利用不可・削除済みの楽曲はキューから除く。明示的なキューUI・シャッフル・リピートは対象外。
- 追加判断: 「前の曲」は再生位置が3秒を超えていれば先頭へ戻し、以内ならひとつ前へ。
  末尾で終端に達したら一時停止表示（位置0）に戻し、再生で先頭から開き直す。
  音量は `path_provider` のアプリケーションサポートディレクトリ配下の `playback_settings.json` に保存し、
  新規パッケージは追加しない。

## 実装

- [x] `AudioPlayerService` に位置/総時間ストリーム・シーク・音量・完了イベントを追加
- [x] `media_kit` 実装で上記APIを提供（`completed` は true のみ通知、音量は 0〜100 へ変換）
- [x] PlayerViewModel: 位置購読・シーク・音量・暗黙キュー（表示中一覧順）・自動次曲送り
- [x] UI: シークバー有効化+時間表示、前後ボタン有効化、音量スライダー
- [x] 音量の永続化（`FilePlaybackSettingsStore`、`main.dart` で起動時に復元）
- [x] `TODO(playback)` の解消
- [x] 2609162153 P2（プレイヤーバー寸法: 左右 290px / 中央 max 540px / gap 18 / 上罫線 0.5px / トラック gray-a4 / 白ノブ 11px+影 / 前後 19px・再生内 16px）
- [x] 1652 / 2155 のヒーロー再生を連続再生（表示中の楽曲リストをキュー）へ切り替え

## テスト

- [x] PlayerViewModelの単体テスト（位置購読・シーク・曲送り・末尾停止・利用不可の除外・音量・復元）
- [x] `FilePlaybackSettingsStore` の単体テスト（往復・壊れたファイル）
- [x] プレイヤーバーのWidgetテスト更新（時間表示・シーク・無効状態・一覧からのキュー・音量）
- [x] ブラウザのWidgetテストにヒーロー再生のキュー検証を追加

## 動作確認

- [x] `flutter analyze` / `flutter test`
- [x] `flutter test integration_test -d macos`（7スイートを個別実行、すべて成功。再生スイートは位置表示・次の曲・末尾停止・音量を追加）
- [x] 実機での目視確認（`tool/design_preview_main.dart` に再生中状態のシードを追加。`verification/player-idle.png` / `player-playing.png` を `01-songs.png` のフッターと並置比較。品質バッジ・シャッフル・リピート・キュー・AirPlay の不在以外に差分なし）
- [ ] Windowsでの確認（macOS環境のため未実施）
